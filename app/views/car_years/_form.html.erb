<%= form_with(model: car_year, local: true) do |form| %>
  <% if car_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car_year.errors.count, "error") %> prohibited this car_year from being saved:</h2>

      <ul>
      <% car_year.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
